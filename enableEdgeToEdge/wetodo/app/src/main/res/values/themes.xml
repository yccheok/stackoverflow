<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeTodo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">#ff0000</item>
        <item name="colorPrimaryVariant">#ff0000</item>

        <item name="appBarOverlay">@style/ThemeOverlay.AppCompat.ActionBar</item>
        <item name="popupOverlay">@style/ThemeOverlay.AppCompat.Light</item>

        <item name="fabBackground">@color/black</item>
        <item name="fabForeground">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>

        <item name="android:navigationBarColor">
            @android:color/transparent
        </item>

        <!-- Optional: set to transparent if your app is drawing behind the status bar. -->
        <item name="android:statusBarColor">
            @android:color/transparent
        </item>

        <item name="primaryTextColor">@color/primaryTextColorLight</item>
        <item name="secondaryTextColor">@color/secondaryTextColorLight</item>
    </style>

    <style name="Theme.WeTodo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>